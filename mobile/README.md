# Mobile Module

This folder contains the target Flutter structure for the monorepo.

- `template_flutter_structure/` is a scaffold for future migration.
- Current production mobile app remains in `../mental_health_app/` for MVP.

Do not place backend Python, SQL migrations, or infra scripts here.
